id: T-060
title: "Khai bằng chứng contract cho cạnh M-lock → M-claim ở đầu nguồn"
serves:
  - G-001
implements: D-007
scope: P-graph-core
status: done
estimated_context: small
model: scribe

context_contract:
  must_read:
    - path: ".ganas/tasks/T-059.yaml"
      why: "Nửa đầu — cạnh và cổng khai ở đầu đích tại đó"
    - path: ".ganas/modules/M-hook-policy.yaml"
      why: "Khuôn khai bằng chứng kind: contract đã chạy được"
  facts: []
  open_questions: []

touches:
  # M-lock là khối T-058 dựng ra; lúc viết task này nó chưa tồn tại nên phải
  # khai tạm M-verify cho qua validate. Thiếu M-lock thì `ownsGanasFile` không
  # stage M-lock.yaml và commit rỗng ruột — đã xảy ra thật ở commit 7a572d1,
  # phải amend. Cùng lớp lỗi với T-058, xem ICE-008.
  - M-lock

exit_contract:
  - kind: command
    run: "npx tsx src/cli.ts validate"
  - kind: command
    run: "npm test"
  - kind: verification
    target: "M-lock/V-lock-probe"

notes: >-
  Chỉ chạy SAU T-059. Mảnh cuối, đúng khuôn T-052 đã đi qua.

  Cạnh `M-lock → M-claim` xuyên phạm vi (nguồn P-graph-core, đích P-hook). Bằng
  chứng `kind: contract` bắt buộc nằm ở khối NGUỒN vì `contractEdges()`
  (`src/graph/trace.ts:44`) lấy `from` chính là khối chứa nó — `M-claim` không
  khai hộ được. Mà task không được chạm hai phạm vi (`validate` chặn cứng bằng
  `scope/task-touches-outside-scope`), nên phải tách ra đây. Xem ICE-012.

  Việc: thêm một mục `verify` với `kind: contract`, `to: M-claim` vào
  `.ganas/modules/M-lock.yaml`, cho cổng `withFileLock` mà M-claim đã khai ở
  `contract.inputs` (T-059).

  `run` đi qua `lintProbe`, lint đối chiếu lệnh với TÊN CỔNG — lệnh chung chung
  sẽ bị `verify/probe-unrelated`. Dùng `grep -qF` chứ không `grep -q`: chữ ký
  thật đầy ký tự regex, mỗi bản grep thoát một kiểu.

  Xong thì `ganas trace` phải về 0 nợ kiểm chứng.

  ĐÃ XẢY RA đúng điều cảnh báo: bản đầu khai `touches: [M-verify]` vì lúc viết
  task thì `M-lock` chưa tồn tại. Hệ quả — `ownsGanasFile` không stage
  `M-lock.yaml`, commit 7a572d1 chỉ mang file task và sổ cái, KHÔNG mang bản
  sửa; `ganas trace` tại HEAD vẫn báo 1 nợ. Đã sửa `touches` thành `M-lock` và
  amend. Cùng lớp lỗi với T-058, xem ICE-008.
done_at: 2026-08-25T10:48:56.447Z
