id: T-010
title: "Hook chặn model tự đặt proposal status: approved"
serves:
  - G-001
implements: D-004
scope: P-hook
status: done
estimated_context: small
model: verifier
blocked_by:
  - T-007

context_contract:
  must_read:
    - path: "src/hooks/handlers.ts"
      why: "denyPreTool + ENTITY_OVERWRITE_REASON: khuôn chặn ghi đã có, thêm luật mới
        đi cùng đường"
  facts: []
  open_questions: []

touches:
  - M-hook-io

exit_contract:
  - kind: command
    run: "npx tsx --test 'test/hooks.test.ts'"
  - kind: command
    run: "npm run typecheck"

  - kind: verification
    target: "M-hook-io/V-hooks-smoke"

notes: >-
  Lớp cứng DUY NHẤT của D-004, cùng lý lẽ đã áp cho decision: duyệt là việc của
  người. Phải đi qua ENFORCEMENT_RULES để dự án cũ tắt riêng được, chứ không
  chặn vô điều kiện.

  BẢN GHI ĐƯỢC SỬA SAU (2026-08-23, T-020): khối `M-hooks` bị chẻ thành
  `M-hook-policy` + `M-hook-io`, nên `touches` và target bằng chứng ở đây được
  trỏ lại sang `M-hook-io` — vùng code T-010 thật sự sửa lúc đó. (Phần hằng số
  lý do sau này chuyển sang `M-hook-policy` là việc của T-020, không phải của
  task này.) Không phải làm đẹp lịch sử: giữ id đã chết thì graph
  đứt và `ganas validate` đỏ vĩnh viễn, còn đây là cùng một vùng code dưới tên
  mới.
done_at: 2026-08-21T10:24:09.336Z
