- id: ICE-028
  title: "Bỏ archive design: .ganas/designs/ sẽ phình dần, chưa đo được cost"
  found_at: 2026-09-01T04:50:38.769Z
  review_after_days: 30
  weight: 2
  ease: 2
  why_deferred: "D-012 định archive design `superseded` sang `designs/archived/`
    rồi bỏ giữa chừng vì cơ chế tự mâu thuẫn: đường duy nhất để một design thành
    `superseded` là được design khác khai `supersedes`, mà archive nó lại làm
    `spine/design-missing-supersede` (validate.ts:278-288) báo lỗi. Muốn chạy
    thật thì loader cộng ba validator phải cùng biết một tập 'đã archive' — hai
    bản đồ song song. Hệ quả để lại: mọi design sống mãi trong graph và được
    parse mỗi lần hook chạy. Hiện 12 design, vài KB, chưa đáng lo. Đáng mở lại
    khi đo được thật: nếu số design lên hàng trăm thì cân nhắc cho `listYaml`
    đọc thêm thư mục con vào một tập chỉ-đọc riêng, thay vì dời file ra khỏi tầm
    mắt của validator."
  anchors:
    - src/graph/load.ts:93-100
    - src/graph/validate.ts:278
  scope: P-graph-core
  status: open
- id: ICE-029
  title: "Khối nature: llm khai verify rỗng thì né được luật bắt buộc có eval"
  found_at: 2026-09-01T05:35:37.727Z
  review_after_days: 30
  weight: 3
  ease: 4
  why_deferred: 'Luật "khối gọi LLM phải có bằng chứng kind: eval" chỉ bắn khi
    verify.length > 0. Khối khai nature: llm với verify: [] rỗng hẳn thì không
    vi phạm gì — chỉ nhận cảnh báo unverified (warning). Tức khai ÍT bằng chứng
    lại NHẸ TỘI hơn khai bằng chứng sai loại, đúng chiều khuyến khích ngược.
    Phát hiện lúc viết ngược exit_contract cho D-001 (2026-09-01) khi đối chiếu
    từng khẳng định của summary với cưỡng chế thật. Chưa vá ngay vì chưa đo được
    tác hại: hiện KHÔNG khối nào trong .ganas/modules/ khai nature: llm, nên khe
    hở đang rỗng người dùng. Đáng mở lại đúng lúc khối llm đầu tiên được khai.
    Cách vá rẻ nhất nếu mở lại: bỏ vế verify.length > 0, cho luật bắn cả khi
    verify rỗng — nhưng phải kiểm trước xem nó có làm mọi khối llm mới tạo đỏ
    ngay từ dòng đầu không.'
  anchors:
    - src/model/module.ts:116
    - src/model/module.ts:104
  scope: P-graph-core
  status: open
- id: ICE-030
  title: Ba chỗ văn xuôi trỏ tới scripts/sync-version.mjs và
    test/version-sync.test.ts — cả hai không tồn tại
  found_at: 2026-09-01T05:35:57.766Z
  review_after_days: 30
  weight: 3
  ease: 5
  why_deferred: Tên thật là release/version.mjs (lệnh con sync/stamp) và
    release/version.test.ts. Ba chỗ còn nhắc tên cũ đã chết. Chỗ đau nhất là
    release/version.test.ts:45 vì đó là THÔNG ĐIỆP LỖI của assertion, chỉ hiện
    lên đúng lúc ai đó bump version mà quên npm run build — rồi chỉ họ chạy
    `node scripts/sync-version.mjs`, một file không tồn tại. Tức hướng dẫn sai
    xuất hiện đúng lúc người ta cần hướng dẫn đúng nhất. CHANGELOG.md:9 nằm
    trong phần đầu file, được đọc mỗi lần mở. Không vá trong lúc đóng D-003 vì
    đó là sửa văn xuôi, nằm ngoài hợp đồng chặng, và gộp vào sẽ làm commit đóng
    chặng lẫn hai loại thay đổi. Sửa chỉ là đổi chuỗi, không đụng logic — ease
    5. Phát hiện 2026-09-01 lúc đối chiếu summary D-003 với code thật.
  anchors:
    - CHANGELOG.md:9
    - release/version.test.ts:23
    - release/version.test.ts:45
  scope: P-release
  status: open
- id: ICE-031
  title: npx ganas chạy bundle plugin/dist đã cũ, nên validate cây làm việc trả
    kết quả sai
  found_at: 2026-09-01T05:45:10.561Z
  review_after_days: 30
  weight: 4
  ease: 3
  why_deferred: "Trong chính repo ganas, `npx ganas <lệnh>` phân giải ra bundle đã
    build (plugin/dist/cli.js, v1.1.0) chứ KHÔNG phải cây làm việc. Sửa src/ rồi
    chạy `ganas validate` thì đang chấm bằng CODE CŨ mà không có dấu hiệu nào
    báo. Đo được ngay trong phiên 2026-09-01 lúc đóng D-013: sau khi sửa luật
    spine/design-orphaned trong src/graph/validate.ts và đóng G-001, `npx ganas
    validate` báo 13 cảnh báo mồ côi (bundle cũ), còn `npx tsx src/cli.ts
    validate` báo 1 (đúng). Chênh 12 cảnh báo, và cái sai là cái trông có thẩm
    quyền hơn. Suýt kết luận bản vá không chạy. Quy ước hiện tại chống đỡ một
    phần: có commit riêng kiểu 'Build lại plugin/dist sau chặng D-xxx' (ef5e456,
    858f009), nhưng nó chỉ chạy CUỐI chặng, trong khi cái bẫy bắn suốt lúc đang
    làm. Hướng đáng cân nhắc: cho lệnh in cảnh báo khi cwd chính là repo ganas
    mà binary đang chạy là bundle, hoặc thêm script npm `dev:validate` rồi sửa
    docs/WORKFLOW.md dùng nó. Chưa vá ngay vì chỉ hại người phát triển CHÍNH
    ganas, không hại dự án dùng ganas — nhưng weight 4 vì nó tạo kết luận sai
    một cách im lặng, đúng lớp lỗi mà dự án này sinh ra để chống."
  anchors:
    - plugin/dist/cli.js
    - plugin/.claude-plugin/plugin.json:4
  scope: P-release
  status: open
- id: ICE-032
  title: flow chặng goal khoá cứng G-001, bảo ghi đè lên chính mục tiêu vừa đóng
  found_at: 2026-09-01T05:46:34.901Z
  review_after_days: 30
  weight: 3
  ease: 4
  why_deferred: "Chặng `goal` của dòng chảy chấm bằng activeGoals().length > 0,
    nên nó bật lên lại mỗi khi mọi goal đã closed — đúng. Nhưng cả `action` lẫn
    `template` khoá cứng chuỗi 'G-001', vốn viết cho dự án TRỐNG vừa chạy ganas
    init. Ở dự án đã đóng được mục tiêu đầu tiên, lời khuyên đó thành 'ghi đè
    lên bản ghi G-001 vừa khép' — mất lịch sử mục tiêu, đúng thứ mà luật
    bản-ghi-trung-thực chống. Đo được ngay 2026-09-01 sau khi đóng G-001: flow
    in 'Sửa .ganas/goals/G-001.yaml thành mục tiêu thật'. Sửa rẻ: dùng `ganas id
    goal` để lấy id kế tiếp (G-002) thay vì hằng số, và đổi lời khi graph đã có
    goal closed thành 'tạo goal mới' chứ không phải 'sửa'. Chưa vá ngay vì chỉ
    bắn ở dự án đã đóng hết goal — hiện mới đúng repo này."
  anchors:
    - src/flow.ts:111
    - src/flow.ts:112
  scope: P-cli
  status: open
- id: ICE-033
  title: Cảnh báo dispatch nổ sai khi phiên chính điều phối task khác task đang bind
  found_at: 2026-09-05T12:26:37.704Z
  review_after_days: 30
  weight: 4
  ease: 3
  why_deferred: "Phát hiện lúc chạy T-082/T-083/T-084 song song trong phiên
    2026-09-05, ngoài phạm vi cả ba task. subagentTouchedFor() trả false khi
    rec.task !== taskId, nên phiên bind T-080 mà chấm gate T-083/T-084 thì cảnh
    báo 'cả phiên không có lượt sửa nào từ sub-agent' nổ SAI — state.json cho
    thấy subagent_touched: true có thật. Chưa ai duyệt cách sửa: hoặc cờ gắn
    theo (session, task) như hiện nay là đúng và cảnh báo phải im khi task chấm
    khác task bind, hoặc cờ nên gắn theo task mà sub-agent thật sự sửa. Đáng chú
    ý vì D-015 (auto-loop) dựng đúng luồng phiên chính điều phối nhiều task, nên
    cảnh báo sai sẽ nổ mỗi vòng lặp."
  anchors:
    - src/state.ts:208
    - src/commands/gate.ts:77
  status: open
- id: ICE-034
  title: Phiên vẫn bind task đã done sau ganas commit, nên Stop hook chấm mãi một
    hợp đồng đã đóng
  found_at: 2026-09-05T13:06:45.393Z
  review_after_days: 30
  weight: 4
  ease: 3
  why_deferred: "Phát hiện 2026-09-05, ngoài phạm vi mọi task đang chạy. Sau khi
    ganas commit T-080 đóng task (status: done) thì state.json vẫn giữ
    current_task: T-080 và sessions[...].task = T-080. Stop hook đọc
    session.task rồi chấm exit_contract của task ĐÃ ĐÓNG — nó đỏ vì lý do hoàn
    toàn không liên quan tới T-080 (khoảng đỏ có chủ ý của T-085 trên
    no-dead-ends), và chặn phiên. Chưa ai duyệt cách sửa: hoặc ganas commit nhả
    bind khi đóng task, hoặc Stop hook bỏ qua task status done. Chú ý: hai cách
    này khác nhau về hệ quả với luồng auto-loop của D-015, nơi phiên chính đóng
    task rồi mở task kế ngay trong cùng phiên."
  anchors:
    - src/hooks/io/handlers.ts:356
    - src/state.ts:143
  status: open
- id: ICE-035
  title: schemaFields() chỉ soi tới khối lồng ĐẦU TIÊN của zConfig, session_start
    và claim tuột khỏi bộ dò ngõ cụt
  found_at: 2026-09-05T13:13:22.560Z
  review_after_days: 30
  weight: 4
  ease: 2
  why_deferred: "Đo được, không suy đoán: chạy chính schemaFields() trên
    src/model/config.ts cho ra 9 tên — version, project, harness, enforcement,
    enforcement_rules, models, auto_loop, auto_begin, ttl_minutes. Thiếu
    session_start và claim; thay vào đó là auto_begin và ttl_minutes, tức trường
    BÊN TRONG chúng. Regex non-greedy dừng ở '})' đầu tiên nên khối ngoài cùng
    bị cắt sớm, các khối lồng sau bị đọc như z.object riêng. Hệ quả: một trường
    lồng thêm vào sau vị trí đó không bao giờ bị bộ dò ngõ cụt soi tới — đúng
    lớp lỗi 'khai rồi không ai đọc' mà test này sinh ra để chặn, và PR-008 đã
    từng vá một ca y hệt (bốn dấu cách thành {2,}). T-087 phải đặt khối
    auto_loop TRƯỚC session_start để nó lọt vào phần được trích; sắp xếp đó đúng
    nhưng dễ vỡ, ai chèn một khối lồng khác lên trước là READ_VIA['auto_loop']
    thành vô nghĩa lặng lẽ. Cách chữa gốc đã được ghi sẵn trong comment của
    chính hàm: dạy schemaFields bỏ qua thân .default(...) thay vì né. Chưa làm
    ngay vì sửa xong nhiều khả năng lộ ra một loạt trường chưa ai đọc, và đó là
    việc riêng chứ không nhét vào chặng D-015 được."
  anchors:
    - test/no-dead-ends.test.ts:126
    - src/model/config.ts:145
  status: open
- id: ICE-036
  title: ganas commit git add TRƯỚC khi recheck, recheck đỏ thì bỏ commit nhưng
    KHÔNG dọn index — commit kế tiếp nuốt
  found_at: 2026-09-05T13:15:16.803Z
  review_after_days: 30
  weight: 5
  ease: 3
  why_deferred: "Đo được 2026-09-05, ngoài phạm vi mọi task đang chạy. Chuỗi thật:
    ganas commit T-086 chạy git add cho ranh giới của nó (src/commit.ts), rồi
    recheck cây sắp commit ĐỎ nên từ chối commit — nhưng index vẫn giữ
    src/commit.ts đã staged. Lệnh kế tiếp ganas commit T-087 commit cả index,
    nên abb3cf8 mang tên T-087 lại chứa src/commit.ts và .ganas/tasks/T-086.yaml
    của T-086. Hàng rào recheck vì thế tự phá mục tiêu của nó: nó chặn đúng một
    commit sai rồi giao nguyên liệu sai cho commit sau. Cách chữa gốc phải dọn
    index khi recheck đỏ (git reset các đường dẫn vừa add, hoặc add sau khi
    recheck xanh). Liên quan nhưng KHÁC ICE-033 và ICE-034 — hai cái đó về bind
    session, cái này về index."
  anchors:
    - src/commands/commit.ts:174
    - commit:abb3cf8
  status: open
